# GitHub summary

## Suggested repository description
Guided is an enterprise-ready Chrome extension starter for resilient in-app training, workflow recording, semantic element resolution, validation, SmartTips, and portable workflow storage.

## Suggested topics
`chrome-extension` `manifest-v3` `digital-adoption-platform` `in-app-guidance` `training` `s3` `backblaze` `azure-blob`

## Initial commit message
`feat: scaffold Guided DAP extension with portable workflow storage`

## Security note
Cloud credentials must never be bundled into the extension. Use a backend gateway with authentication and short-lived presigned URLs.
